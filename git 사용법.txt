git init

git add *  ||  git add index.html

git config --global user.email "auction4289@gmail.com"

git config --global user.name "taejeong123"

git commit -m "커밋 이름"

git remote add origin <git 주소>

git push origin master

======================================

클론 후 수정 완료

클론 후 다운로드 버튼 누름

git pull origin

master 브랜치로 변경하기

git checkout master
